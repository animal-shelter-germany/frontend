name: Server Deploy

on: 
  push:
    branches: [main]

jobs:
  build_and_deploy:
    runs-on: [self-hosted, Linux, x64, animal-shelter]
    steps: 
      - name: Build Images
        run: docker compose down
      - name: Build Images
        run: docker compose up -d --build
